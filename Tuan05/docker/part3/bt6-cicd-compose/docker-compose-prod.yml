services:
  app-prod:
    build: ./app
    container_name: bt6_prod_app
    command: python app.py

  nginx:
    image: nginx:latest
    container_name: bt6_prod_nginx
    depends_on:
      - app-prod
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
